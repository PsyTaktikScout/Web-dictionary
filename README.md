# Web Dictionary

A lightweight web app that reads and displays data from a Google Sheet in a table. Built with vanilla JavaScript.

## How it works

1. The app fetches row data from a public Google Sheet via the Sheets API
2. Renders the data in an HTML table on page load

## Usage

Open `index.html` in a browser — no build step needed.

## License

MIT
